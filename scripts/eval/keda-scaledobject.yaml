# Day 14 placeholder — KEDA ScaledObject (filled in Day 14 execution)
# This is a stub; the actual ScaledObject depends on the KEDA version
# installed and the Prometheus query chosen during Day-14 evaluation.
#
# After KEDA install (helm install keda kedacore/keda -n keda --create-namespace),
# apply something like:
#
# apiVersion: keda.sh/v1alpha1
# kind: TriggerAuthentication
# metadata:
#   name: keda-prometheus-auth
#   namespace: keda
# spec:
#   secretTargetRef:
#     - parameter: bearerToken
#       name: prometheus-bearer-token
#       key: token
# ---
# apiVersion: keda.sh/v1alpha1
# kind: ScaledObject
# metadata:
#   name: podinfo-keda
#   namespace: podinfo
# spec:
#   scaleTargetRef:
#     name: podinfo
#   minReplicaCount: 2
#   maxReplicaCount: 10
#   cooldownPeriod: 60
#   triggers:
#   - type: prometheus
#     metadata:
#       serverAddress: http://kube-prometheus-stack-prometheus.monitoring.svc.cluster.local:9090
#       query: |
#         sum(rate(http_requests_total{service="podinfo"}[1m])) * 5
#       threshold: "15"
#       activationThreshold: "5"

apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: podinfo-keda-placeholder
  namespace: podinfo
spec:
  scaleTargetRef:
    name: podinfo
  minReplicaCount: 2
  maxReplicaCount: 10
  cooldownPeriod: 60
  triggers:
    - type: prometheus
      metadata:
        serverAddress: http://placeholder.invalid
        query: "vector(0)"
        threshold: "0"