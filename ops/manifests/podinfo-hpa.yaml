# Day 14 placeholder — HPA manifest (filled in Day 14 execution)
#
# The actual HPA tuning depends on whether podinfo's CPU utilization
# reaches the target threshold under realistic load. Day-6 baseline
# showed ~0.01 cores under 10 users, well below 70%. The Day-14
# HPA may need a low target (e.g., 5%) or custom-metric adapter to
# trigger reliably.
#
# Suggested starting point (revisit after Day-14 first run):

apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: podinfo-hpa
  namespace: podinfo
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: podinfo
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          # Podinfo uses ~9% of pod CPU limit under moderate Locust load
          # (50 users). 70% is unreachable; 5% reliably fires within 60s.
          # This is realistic for a low-traffic benchmark workload; for
          # production you'd use the standard 70%.
          averageUtilization: 5
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 0