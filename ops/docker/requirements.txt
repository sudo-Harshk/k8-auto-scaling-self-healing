# Shared Python dependencies for the AI-Driven Kubernetes Operator project.
#
# Installed into the shared Docker image ops/docker/Dockerfile (python:3.11-slim base).
# Day-by-day additions are noted with comments so the dependency timeline is traceable.
#
# Pinned to versions that ship prebuilt wheels for Python 3.11 / linux/amd64, so the
# Docker build does not require a C toolchain and stays fast.

# ----- Day 3: metrics client + load test -----
prometheus-api-client==0.5.5
requests==2.32.3
locust==2.31.1
pandas==2.2.3
numpy==2.4.6

# ----- Day 4: Kafka producer/consumer -----
kafka-python==2.0.2

# ----- Day 5: Faust stream processor -----
faust-streaming==0.11.3
aiokafka==0.10.0

# ----- Day 7: River online ML (replica predictor + anomaly detector) -----
river==0.26.0

# Future days add deps as components come online:
#   Day 9: shap==0.46.0, scikit-learn   (decision explainability)
#  Day 12: kopf==1.37.2, kubernetes     (Kopf operator + k8s client)